<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>歴代iPhone</title>
</head>
<body>
  <h2>詳細(発売日順)</h2>
  <p>※値段は最も安いものを表示</p>
  <table>
    <tr><th>ID</th><th>名前</th></tr>
      <% let number = 0 %>
      <% for( let row of data ) { %>
        <tr><td><%= row.id %></td>
        <td><a href="/app/<%= number %>"><%= row.name %></a></td></tr>
        <% number += 1 %>
      <% } %>
  </table>
  <a href="/app/create">追加</a>
</body>
</html>